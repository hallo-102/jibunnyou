# Phase 10 Loop 1〜3 テスト・品質改善 受入検証

- 実施日: 2026-07-11
- 判定: 合格
- 自己評価: 97/100

## 自動テスト

| 対象 | 結果 |
|---|---:|
| Host Backend全体 | 71 passed / 57.51秒 |
| Container Backend全体 | 71 passed / 124.58秒 |
| Frontend UI契約 | 4 passed |
| Frontend production build | 成功 |
| PowerShell backup/restore構文 | 成功 |

Backendは設定、DB、migration、API、file input、Collector、Python予想、AI独立、AI比較統合、買い目、結果精算、成績、異常系、冪等性、安全契約を含む。UI契約は6領域navigation、次操作、loading/error、keyboard、買い目安全表示、mobile/printを固定した。

## バックアップ・復元試験

1. 稼働PostgreSQLをcustom formatでdatabase-only backupした。
2. Manifestと全SHA-256を生成・検証した。
3. 稼働DBを変更せず、隔離DB `keiba_restore_acceptance_20260711`へ復元した。
4. Alembic revision `0005_result_analytics`が一致した。
5. 主要件数が一致した。

| table | 稼働DB | 復元DB |
|---|---:|---:|
| races | 36 | 36 |
| prediction_runs | 3 | 3 |
| bet_candidates | 6 | 6 |
| audit_logs | 20 | 20 |

検証後、隔離DBだけを削除し、稼働DBとバックアップを保持した。

## Docker停止・再起動試験

- 全7サービスを停止。
- PostgreSQL/Redis → API/Worker/Beat → Frontend → Gatewayの順に起動。
- PostgreSQL、Redis、API、Frontendのhealthが正常。
- `/api/ready`はpostgres/redis/artifact/configがすべて`ok`。
- `/api/health`、`/api/version`、画面HTTP 200を確認。
- Workerでcollector、prediction、AI独立、AI比較統合、pingの5タスク登録を確認。
- 公開portはGateway `18080`だけで、他6サービスは内部限定。

## 不具合と修正

初回Container回帰では、安全契約テストがルート契約ファイルを読めず2件失敗した。実行時秘密を含まない`.gitignore`、`.env.example`、Compose、Frontend appだけを`/workspace/release-contract`へ同梱し、Host/Container共通テストへ修正した。修正後は71件全合格。

## 完了判定

主要テスト、回帰、異常系、Docker build、停止・再起動、バックアップ・隔離復元、秘密情報、自動購入禁止に合格し、重大・高優先度の未修正不具合は0件。Phase 11へ進む。
